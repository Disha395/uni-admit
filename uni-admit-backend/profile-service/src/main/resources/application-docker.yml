spring:
  datasource:
    url: jdbc:postgresql://profile-postgres:5432/profile_db
    username: postgres
    password: postgres

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8070/eureka/