spring:
  datasource:
    url: jdbc:postgresql://auth-postgres:5432/auth_db
    username: postgres
    password: postgres
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8070/eureka/